apiVersion: v1
kind: Service
metadata:
  name: apuestas-service-lb
  labels:
    app: apuestas-service
spec:
  type: LoadBalancer          
  selector:
    app: apuestas-service       
  ports:
    - name: http
      protocol: TCP
      port: 80                
      targetPort: 8005